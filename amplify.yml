version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static site - no build required"
  artifacts:
    # Include all files from the root directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
